add_custom_target(packaging ALL DEPENDS CrealityPrint COMMENT "Packaging Projects...")
IF(APPLE)
    include(macx.cmake)
elseif (WIN32)
    message(STATUS "Windows Packaging")
    if(NOT DEFINED PACKAGE_CONF)
        set(PACKAGE_CONF package.cmake)
    endif()
    include(${PACKAGE_CONF})
endif()